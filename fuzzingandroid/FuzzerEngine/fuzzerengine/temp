emma report usage: missing required option 'in'

emma report usage: emma report [options],
  where options include:

  -in, -input <list of files>
                {required} list of meta/coverage data files
  -r, -report <list of {txt|html|lcov|xml}>
                {required} coverage report type list
  -sp, -sourcepath <list of source directories>
                Java source path for generating reports
  -p, -props, -properties <properties file>
                properties override file
  -h, -help     display usage information

  {use 'help' option to see detailed usage help}

[EMMA v0.0, build 0 (unsupported private build)]
